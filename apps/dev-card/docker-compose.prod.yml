version: '3.8'

services:
  web3insight-dev-card:
    image: web3insight-dev-card:latest
    container_name: web3insight-dev-card-prod
    restart: always
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3000/"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - web3insight-dev-card-network

networks:
  web3insight-dev-card-network:
    driver: bridge
